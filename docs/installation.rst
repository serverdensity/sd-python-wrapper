.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install sd-python-wrapper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sd-python-wrapper
    $ pip install sd-python-wrapper
